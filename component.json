{
  "name": "is-host",
  "repo": "johntron/is-host",
  "description": "Loosely validate a network host address",
  "version": "0.0.1",
  "keywords": ["validate", "host", "address", "regexp", "regex"],
  "license": "MIT",
  "scripts": ["index.js"]
}
